<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorHighlight</item>
    </style>

    <style name="ThemeLight" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="colorAccent">@color/colorHighlight</item>
        <item name="android:textColorPrimary">@color/colorHighlightDark</item>
    </style>

    <style name="ThemeDark" parent="Theme.AppCompat.NoActionBar" />

    <style name="TextAppearance.ThemeLight.Title" parent="Base.TextAppearance.AppCompat">
        <item name="android:textColor">@color/colorTitle</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">19sp</item>
    </style>

</resources>
